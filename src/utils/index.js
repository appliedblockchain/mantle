const {
  bufferToHex,
  bufferToHex0x,
  bufferToOther,
  bytesToBuffer
} = require('./conversions')

const utils = {
  bufferToHex,
  bufferToHex0x,
  bufferToOther,
  bytesToBuffer
}

module.exports = utils
